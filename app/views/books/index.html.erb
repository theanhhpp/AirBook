<% provide :title, "Shop"%>
<%= render 'home/sidebar'%>

<div class="col-sm-9 padding-right">
	<div class="features_items">
	<%= render partial: 'book_area', locals: { books: @books } %>
	</div>
</div>
			</div>
		</div>
	</section>